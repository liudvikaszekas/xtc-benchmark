"""
Helper script to run Hungarian matching on generated OpenPSG scene graphs.

This script loads the scene graphs generated by generate_openpsg_graphs.py
and runs the semantic graph matching algorithm on each image pair.
"""

import json
import argparse
import tempfile
import os
from pathlib import Path
from typing import Dict, List
import numpy as np

# Avoid CUDA issues by using CPU for sentence transformers
os.environ['CUDA_VISIBLE_DEVICES'] = ''
# Avoid tokenizer parallelism issues when forking child processes
os.environ['TOKENIZERS_PARALLELISM'] = 'false'

from hungarian import (
    load_graph, 
    compute_text_embeddings_for_graph,
    semantic_graph_matching, 
    convert_hungarian_graph_to_nx, 
    node_subst_cost, 
    edge_subst_cost
)
from sentence_transformers import SentenceTransformer
import time
import traceback
import sys
import networkx as nx


def load_scene_graphs(json_path: str) -> Dict:
    """Load scene graphs from JSON file."""
    print(f"Loading scene graphs from {json_path}...")
    with open(json_path, 'r') as f:
        data = json.load(f)
    return data


def match_scene_graphs(
    scene_graphs_data: Dict,
    model: SentenceTransformer,
    high_recall_mode: bool = True,
    temp_dir: str | None = None
) -> Dict:
    """
    Run Hungarian matching on all image pairs.
    
    Args:
        scene_graphs_data: Loaded scene graphs JSON
        model: SentenceTransformer model for embeddings
        high_recall_mode: Whether to use high recall mode in matching
        temp_dir: Directory for temporary files (created if None)
    
    Returns:
        Dictionary with matching results for each image
    """
    if temp_dir is None:
        temp_dir = tempfile.mkdtemp()
    
    results = {}
    images = scene_graphs_data['images']
    
    print(f"\nMatching {len(images)} image pairs...")
    print(f"High recall mode: {high_recall_mode}")
    
    from tqdm import tqdm
    for image_id, graphs in tqdm(images.items(), desc="Matching Progress"):
        try:
            # Save graphs to temporary files
            temp_pred = os.path.join(temp_dir, f'temp_pred_{image_id}.json')
            temp_gt = os.path.join(temp_dir, f'temp_gt_{image_id}.json')

            with open(temp_pred, 'w') as f:
                json.dump(graphs['prediction'], f)
            with open(temp_gt, 'w') as f:
                json.dump(graphs['ground_truth'], f)

            # Load with hungarian.py
            pred_graph = load_graph(temp_pred)
            gt_graph = load_graph(temp_gt)

            # Compute embeddings
            compute_text_embeddings_for_graph(pred_graph, model)
            compute_text_embeddings_for_graph(gt_graph, model)

            # Convert to NetworkX format
            pred_nx = convert_hungarian_graph_to_nx(pred_graph)
            gt_nx = convert_hungarian_graph_to_nx(gt_graph)

            # Run matching (Hungarian) directly
            try:
                match_results = semantic_graph_matching(gt_nx, pred_nx, model)
            except Exception as e:
                # Still log errors
                tqdm.write(f"    Matching error for image {image_id}: {e}")
                traceback.print_exc()
                results[image_id] = None
                # attempt cleanup and continue
                try:
                    os.remove(temp_pred)
                    os.remove(temp_gt)
                except Exception:
                    pass
                continue

            if match_results:
                precision, recall, f1, matched_edges, unmatched_gt, unmatched_pred, \
                matched_nodes, unmatched_gt_nodes, unmatched_pred_nodes = match_results

                results[image_id] = {
                    'precision': float(precision),
                    'recall': float(recall),
                    'f1_score': float(f1),
                    'num_matched_edges': len(matched_edges),
                    'num_unmatched_gt_edges': len(unmatched_gt),
                    'num_unmatched_pred_edges': len(unmatched_pred),
                    'num_matched_nodes': len(matched_nodes),
                    'num_unmatched_gt_nodes': len(unmatched_gt_nodes),
                    'num_unmatched_pred_nodes': len(unmatched_pred_nodes),
                    'num_gt_edges': len(graphs['ground_truth']['edges']),
                    'num_pred_edges': len(graphs['prediction']['edges']),
                    'num_gt_nodes': len(graphs['ground_truth']['nodes']),
                    'num_pred_nodes': len(graphs['prediction']['nodes']),
                    'matched_node_pairs': matched_nodes,  # Save for visualization
                    'unmatched_gt_nodes': unmatched_gt_nodes,  # Save unmatched GT nodes
                    'unmatched_pred_nodes': unmatched_pred_nodes,  # Save unmatched predicted nodes
                    'matched_edge_pairs': matched_edges,  # Save matched edges
                    'unmatched_gt_edges': unmatched_gt,  # Save unmatched GT edges
                    'unmatched_pred_edges': unmatched_pred,  # Save unmatched pred edges
                }
            else:
                results[image_id] = None

            # Clean up temp files
            try:
                os.remove(temp_pred)
                os.remove(temp_gt)
            except Exception:
                pass

        except Exception as e:
            # Still log errors but keep tqdm working
            tqdm.write(f"  Error processing image {image_id}: {e}")
            results[image_id] = None
    
    return results

    
    return results


def compute_aggregate_metrics(results: Dict) -> Dict:
    """Compute aggregate metrics across all images."""
    valid_results = [r for r in results.values() if r is not None]
    
    if not valid_results:
        return {}
    
    # Compute means for Hungarian algorithm
    mean_precision = np.mean([r['precision'] for r in valid_results])
    mean_recall = np.mean([r['recall'] for r in valid_results])
    mean_f1 = np.mean([r['f1_score'] for r in valid_results])
    
    # Compute totals
    total_matched_edges = sum(r['num_matched_edges'] for r in valid_results)
    total_gt_edges = sum(r['num_gt_edges'] for r in valid_results)
    total_pred_edges = sum(r['num_pred_edges'] for r in valid_results)
    
    total_matched_nodes = sum(r['num_matched_nodes'] for r in valid_results)
    total_gt_nodes = sum(r['num_gt_nodes'] for r in valid_results)
    total_pred_nodes = sum(r['num_pred_nodes'] for r in valid_results)
    
    # Compute overall precision/recall (micro-average)
    total_fp = total_pred_edges - total_matched_edges
    total_fn = total_gt_edges - total_matched_edges
    
    overall_precision = total_matched_edges / total_pred_edges if total_pred_edges > 0 else 0.0
    overall_recall = total_matched_edges / total_gt_edges if total_gt_edges > 0 else 0.0
    overall_f1 = 2 * (overall_precision * overall_recall) / (overall_precision + overall_recall) \
                 if (overall_precision + overall_recall) > 0 else 0.0
    
    return {
        'num_images': len(valid_results),
        'mean_precision': float(mean_precision),
        'mean_recall': float(mean_recall),
        'mean_f1': float(mean_f1),
        'overall_precision': float(overall_precision),
        'overall_recall': float(overall_recall),
        'overall_f1': float(overall_f1),
        'total_matched_edges': total_matched_edges,
        'total_gt_edges': total_gt_edges,
        'total_pred_edges': total_pred_edges,
        'total_matched_nodes': total_matched_nodes,
        'total_gt_nodes': total_gt_nodes,
        'total_pred_nodes': total_pred_nodes,
        'total_false_positives': total_fp,
        'total_false_negatives': total_fn,
    }


def main():
    parser = argparse.ArgumentParser(
        description="Run Hungarian matching on OpenPSG scene graphs"
    )
    parser.add_argument(
        '--input',
        type=str,
        required=True,
        help='Path to scene graphs JSON file (from generate_openpsg_graphs.py)'
    )
    parser.add_argument(
        '--output',
        type=str,
        default=None,
        help='Output path for matching results JSON (default: input_path with _matched.json)'
    )
    parser.add_argument(
        '--model',
        type=str,
        default='all-MiniLM-L6-v2',
        help='SentenceTransformer model for embeddings'
    )
    parser.add_argument(
        '--high-recall',
        action='store_true',
        default=True,
        help='Use high recall mode (forces all GT edges to be matched)'
    )
    parser.add_argument(
        '--no-high-recall',
        action='store_true',
        help='Disable high recall mode'
    )
    
    args = parser.parse_args()
    
    # Determine output path
    if args.output is None:
        input_path = Path(args.input)
        args.output = str(input_path.parent / f"{input_path.stem}_matched.json")
    
    # Load scene graphs
    scene_graphs = load_scene_graphs(args.input)
    
    # Initialize model
    print(f"\nLoading embedding model: {args.model}")
    model = SentenceTransformer(args.model)
    
    # Determine high recall mode
    high_recall = args.high_recall and not args.no_high_recall
    
    # Run matching
    results = match_scene_graphs(scene_graphs, model, high_recall_mode=high_recall)
    
    # Compute aggregate metrics
    aggregate = compute_aggregate_metrics(results)
    
    # Prepare output
    output_data = {
        'metadata': scene_graphs.get('metadata', {}),
        'matching_config': {
            'model': args.model,
            'high_recall_mode': high_recall,
        },
        'aggregate_metrics': aggregate,
        'per_image_results': results
    }
    
    # Save results
    with open(args.output, 'w') as f:
        json.dump(output_data, f, indent=2)
    
    # Print summary
    print(f"\n{'='*70}")
    print("Matching Complete!")
    print(f"{'='*70}")
    print(f"Results saved to: {args.output}")
    
    if aggregate:
        print(f"\nAggregate Metrics ({aggregate['num_images']} images):")
        print(f"\n{'='*70}")
        print("HUNGARIAN ALGORITHM RESULTS:")
        print(f"{'='*70}")
        print(f"  Mean Precision:    {aggregate['mean_precision']:.4f}")
        print(f"  Mean Recall:       {aggregate['mean_recall']:.4f}")
        print(f"  Mean F1:           {aggregate['mean_f1']:.4f}")
        print(f"\n  Overall Precision: {aggregate['overall_precision']:.4f}")
        print(f"  Overall Recall:    {aggregate['overall_recall']:.4f}")
        print(f"  Overall F1:        {aggregate['overall_f1']:.4f}")
        
        print(f"\n{'='*70}")
        print(f"Match Counts:")
        print(f"{'='*70}")
        print(f"  Edges Matched:     {aggregate['total_matched_edges']}")
        print(f"  Edges GT:          {aggregate['total_gt_edges']}")
        print(f"  Edges Predicted:   {aggregate['total_pred_edges']}")
        print(f"  - - - - - - - - - - - - - - - - - - ")
        print(f"  Nodes Matched:     {aggregate['total_matched_nodes']}")
        print(f"  Nodes GT:          {aggregate['total_gt_nodes']}")
        print(f"  Nodes Predicted:   {aggregate['total_pred_nodes']}")
        print(f"\n  False Positives:   {aggregate['total_false_positives']}")
        print(f"  False Negatives:   {aggregate['total_false_negatives']}")


if __name__ == '__main__':
    main()
